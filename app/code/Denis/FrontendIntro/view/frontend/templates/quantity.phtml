<?php
    if ($currentProduct = $block->getCurrentProduct()) {
        $idProduct = $currentProduct->getId();
        $stockProduct = $block->getStockItem($idProduct);
        echo 'Quantity: ' . $stockProduct->getQty() . '<br>';
    }     
?>